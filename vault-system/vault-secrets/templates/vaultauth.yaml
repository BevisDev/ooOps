apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultAuth
metadata:
  name: default
  namespace: {{ .namespace }}
spec:
  method: kubernetes
  mount: kubernetes
  kubernetes:
    role: {{ .role }}
    serviceAccount: vault-sa
  