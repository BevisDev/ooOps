{{- $root := . }}
{{- with .Values.brokers }}
apiVersion: kafka.strimzi.io/v1
kind: KafkaNodePool
metadata:
  name: brokers
  labels:
    {{- include "kafka.labels" $root | nindent 4 }}
  {{- with .annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  replicas: {{ required "brokers.replicas is required" .replicas }}
  roles:
    - broker
  storage:
    type: {{ required "brokers.storage.type is required" .storage.type }}
    {{- if eq .storage.type "persistent-claim" }}
    size: {{ required "brokers.storage.size is required" .storage.size }}
    deleteClaim: {{ .storage.deleteClaim | default false }}
    {{- with .storage.class }}
    class: {{ . }}
    {{- end }}
    {{- with .storage.id }}
    id: {{ . }}
    {{- end }}
    {{- end }}
  {{- with .resources }}
  resources:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  {{- with .jvmOptions }}
  jvmOptions:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  {{- with .template }}
  template:
    {{- tpl (toYaml .) $root | nindent 4 }}
  {{- end }}
{{- end }}
