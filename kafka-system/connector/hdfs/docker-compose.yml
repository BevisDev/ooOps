networks:
  kafka_kafka-net:
    external: true

services:
  connect:
    image: confluentinc/cp-kafka-connect:8.1.0
    container_name: connect
    ports:
      - "8083:8083"
    networks:
      - kafka_kafka-net
    environment:
      CONNECT_BOOTSTRAP_SERVERS: "kafka:29092"
      CONNECT_REST_ADVERTISED_HOST_NAME: "connect"
      CONNECT_REST_PORT: 8083
      CONNECT_GROUP_ID: "connect-cluster"

      CONNECT_CONFIG_STORAGE_TOPIC: "connect-configs"
      CONNECT_OFFSET_STORAGE_TOPIC: "connect-offsets"
      CONNECT_STATUS_STORAGE_TOPIC: "connect-status"

      CONNECT_CONFIG_STORAGE_REPLICATION_FACTOR: 1
      CONNECT_OFFSET_STORAGE_REPLICATION_FACTOR: 1
      CONNECT_STATUS_STORAGE_REPLICATION_FACTOR: 1

      CONNECT_KEY_CONVERTER: "org.apache.kafka.connect.json.JsonConverter"
      CONNECT_VALUE_CONVERTER: "org.apache.kafka.connect.json.JsonConverter"

      CONNECT_INTERNAL_KEY_CONVERTER: "org.apache.kafka.connect.json.JsonConverter"
      CONNECT_INTERNAL_VALUE_CONVERTER: "org.apache.kafka.connect.json.JsonConverter"

      CONNECT_PLUGIN_PATH: "/usr/share/java,/usr/share/confluent-hub-components"

      # =============== LOGGING =================
      CONNECT_LOG4J_ROOT_LOGLEVEL: INFO
      CONNECT_LOG4J_LOGGERS: >
        org.apache.kafka.connect.runtime.Worker=INFO,
        org.apache.kafka.connect.runtime.WorkerSinkTask=INFO,
        org.apache.kafka.connect.runtime.distributed.DistributedHerder=INFO,
        io.confluent.connect.hdfs=INFO
      CONNECT_LOG4J_TOOLS_LOGLEVEL: ERROR

      # HDFS for authenticate kerberos
      # KAFKA_OPTS: "-Djava.security.krb5.conf=/etc/kafka/kerberos/krb5.conf"
    restart: unless-stopped
    logging:
      driver: "json-file"
      options:
        max-size: "100m"
        max-file: "10"
    volumes:
      # download plugins and mount to container
      # Read more: README
      - ../plugins:/usr/share/confluent-hub-components
      # for authenticate kerberos
      # === include: krb5.conf and key.keytab
      # - ../kerberos:/etc/kafka/kerberos
