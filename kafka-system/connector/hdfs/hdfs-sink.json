{
  "name": "hdfs-sink",
  "config": {
    "tasks.max": "3",
    "flush.size": "5000", //  = message kafka
    "rotate.interval.ms": "600000", // msg đầu tiên ghi vào => 10phút sẽ đóng file lại
    "rotate.schedule.interval.ms": "900000", // 30p sẽ force đóng lại file
    "topics.regex": ".*events$",
    "connector.class": "io.confluent.connect.hdfs.HdfsSinkConnector",
    "hdfs.url": "hdfs://namenode:9000",
    "topics.dir": "/kafka-data",
    "logs.dir": "/kafka-logs",
    "format.class": "io.confluent.connect.hdfs.json.JsonFormat",
    "partitioner.class": "io.confluent.connect.hdfs.partitioner.TimeBasedPartitioner",
    "path.format": "YYYY-MM-dd/HH",
    "partition.duration.ms": "3600000",
    "storage.class": "io.confluent.connect.hdfs.storage.HdfsStorage",
    "key.converter": "org.apache.kafka.connect.storage.StringConverter",
    "value.converter": "org.apache.kafka.connect.json.JsonConverter",
    "value.converter.schemas.enable": "false",
    "locale": "en_US",
    "timezone": "Asia/Ho_Chi_Minh",
    "errors.tolerance": "all",
    "errors.log.enable": "true",
    "errors.log.include.messages": "true"
    // "hdfs.authentication.kerberos": "true",
    // "connect.hdfs.principal": "user@COMPANY.VN",
    // "connect.hdfs.keytab": "/etc/kafka/kerberos/key.keytab"
  }
}
